# Post drafts

Markdown drafts of the public post live here. This directory is scanned by
`ci/check_measured_numbers.py`: any line containing a digit must carry a
`measured: <source>` tag, the `MEASURED_TBD` placeholder, or an explicit
`<!-- no-number -->` marker for non-claim numbers. See spec §8 and kickoff
agreement "measured numbers only".

(This README is not a draft and contains no numeric claims.)
