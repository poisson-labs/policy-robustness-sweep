# G2 smoke run — measurements

All numbers measured from run `20260813T154526Z-smoke` (Modal app
ap-GAY2FHUcIp1JKuqjsmq8rM, workspace <modal-workspace>); sources: the run's
`summary.json` / `metrics.jsonl` on Volume `opw-checkpoints` under
`runs/20260813T154526Z-smoke/`, mirrored to this repo's PR.

- Wall time (inside-function, image already built): 152.3 s
  (measured: summary.json wall_seconds)
- Steps executed: 3,276,800 (requested 2,000,000; brax rounds up to its
  training-step multiple) (measured: summary.json num_timesteps → final metrics step)
- Training throughput: 5,771,790 steps/s (measured: metrics.jsonl training/sps)
- Eval throughput: ~7,073–7,662 steps/s (measured: metrics.jsonl eval/sps, both evals)
- First eval (step 0) walltime: 18.1 s — this is JIT-compile dominated
  (measured: metrics.jsonl eval/walltime at step 0)
- Image build (with git + baked Menagerie assets): reported by an earlier
  iteration's build at 36.22 s without Menagerie bake; the final image's build time was
  not separately captured — MEASURED_TBD (rebuild log)
- Cost: MEASURED_TBD (to be read from the Modal dashboard/bill for app
  ap-GAY2FHUcIp1JKuqjsmq8rM; per-second billed duration × published rate is NOT used —
  receipt numbers come from the bill)
- Checkpoints written: step_000000000000 (untrained "newborn" — confirms brax's
  policy_params_fn fires at step 0), step_000003276800, converged
  (measured: summary.json checkpoints)
- Resolved environment (pins source): brax==0.14.2, jax==0.9.2, jaxlib==0.9.2,
  mujoco==3.11.0, mujoco-mjx==3.11.0, playground==0.2.0
  (measured: resolved-environment.txt from the run)
