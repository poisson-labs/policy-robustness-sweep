# G2 full training run — measurements

All numbers measured from run `20260813T155006Z` (Modal, workspace <modal-workspace>,
A100-80GB); sources: the run's `summary.json` / `metrics.jsonl` on Volume
`opw-checkpoints` under `runs/20260813T155006Z/` (metrics mirrored in this directory as
`2026-08-13-g2-full-run-metrics.jsonl`).

## Training

- Steps: 217,907,200 executed (200,000,000 requested; brax rounds up)
  (measured: summary.json / final metrics step)
- Wall time, inside-function: 736.2 s ≈ 12.3 min — includes JIT compile and 20 eval
  passes (measured: summary.json wall_seconds)
- Final eval episode reward: 28.27; final avg episode length: 1000.0 of 1000 cap —
  no falls in final eval (measured: metrics.jsonl, last row)
- Learning curve: reward 0.001 (step 0, avg length 30) → 13.59 (first segment,
  step 11.47M) → plateau ≈ 27–28 from ~step 90M on (measured: metrics.jsonl)
- Cost: **$0.511, list-derived, not billed** — 736.2 s of A100-80GB at $0.000694/s
  (measured: summary.json wall_seconds × docs/modal-verification.md §3 rate). Billing read
  2026-09-15 (DEVLOG Session 31): the bill is per cycle, not per app-run — cycle
  Aug 1 – Sep 1 2026 was $40.14 usage / $10.14 after credits, all work included

## Checkpoints (21 on Volume) + stage labels

Stage labels (stage-labels.json on the Volume; selection rule = train/stages.py,
wobbly = eval reward nearest 50% of final):

- newborn: step_000000000000 (untrained; eval reward 0.001, avg length 30 — falls
  immediately)
- wobbly: step_000011468800 (eval reward 13.59 ≈ half of final 28.27)
- converged: step_000217907200 (= the `converged` save)

Note: the learning curve is steep enough that half-of-final performance falls within the
FIRST eval segment (11.47M steps ≈ 5.3% of training). Checkpoint cadence was 20 segments;
no finer intermediate exists below 11.47M steps except step 0 (unrecoverable — recorded
so nobody goes looking).

## DR record (spec §8 reference)

Verbatim capture committed as `docs/dr-record-go1-training.json` (full source text +
sha256 of `_src/locomotion/go1/randomize.py` and `config/locomotion_params.py`, package
versions playground 0.2.0 / brax 0.14.2 / jax 0.9.2 / mujoco + mjx 3.11.0). Training-time
floor-friction randomization band: U(0.4, 1.0) — the inside/outside-DR reference line for
the sweep's friction axis.

## Nominal verification (deterministic policy, fixed eval seed 0)

Source: `verify-converged.json` (Volume run dir; mirrored here as
`2026-08-13-g2-verify-converged.json`), produced by `train/verify_nominal.py`.

- 8/8 episodes ran the full 1000-step cap — zero falls (measured: lengths)
- Episode reward: mean 30.30, std 0.54, min 29.61, max 31.54 (measured: stats)
- Context: deterministic-policy reward sits slightly above the stochastic training
  eval's final 28.27 — consistent, not contradictory; G3 equivalence will use the same
  stochastic eval protocol as training for its comparison
- Sanity clip: `nominal-gait-converged.mp4` (Volume run dir), rendered via EGL,
  932-frame first episode at 640×480
- Verification run cost: **not separable, and now permanently so** — `verify_nominal.py`
  recorded no wall time, and the billing read of 2026-09-15 (DEVLOG Session 31) returned a
  per-cycle total, not a per-run one. Neither a measured nor a list-derived figure can be
  produced for this run. The debt is closed as unrecoverable rather than left open
